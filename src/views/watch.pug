extends base

block content 
    video(src="/"+video.fileUrl,controls, width="500", height="500")
    div 
        button#play Play 
        button#mute Mute 
        span#time 00:00/00:00
        input(type="range",step=0.1, value=0.5, min=0, max=1)#volmun 
        
    //-
        div
            p=video.description
            small=video.createdAt
            div 
                small Upload by 
                    a(href=`/users/${video.owner._id}`)=video.owner.name
                
        if String(video.owner._id)===loggedInUser._id
            a(href=`${video.id}/edit`) Edit 
            br
            a(href=`${video.id}/delete`) Delete
block scripts 
    script(src="/static/js/videoPlayer.js")    