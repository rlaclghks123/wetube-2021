extends base
include mixins/video   

block content 
    div.profile.data
        img(src="/"+user.avatar_url).avatar
        h3=user.name
    div.video__grid
        each video in user.videos
            +video(video)
        else 
            li Sorry nothing Found 