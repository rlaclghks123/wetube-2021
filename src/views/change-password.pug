extends base

block content 
    if errorMessage
        span=errorMessage
    form(method="post")
        input(placeholder="Oldpassword" type="password" name="oldpassword")
        input(placeholder="New password" type="password" name="newpassword")
        input(placeholder="Confirm password" type="password" name="newpassword1")
        input(type="submit", value="Change password")